<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  controllerName="sps.wms.controller.etiquetavolume"
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:data="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
  xmlns:layout="sap.ui.layout"
  xmlns:f="sap.ui.layout.form"
  xmlns:t="sap.ui.table"
>
  <Page
    id="pageView"
    busyIndicatorDelay="0"
    showHeader="true"
    title="Re/Impressão Etiqueta : {Data>/CompanyName}"
    showFooter="true"
    showNavButton="true"
    navButtonPress="onBack"
  >
    <headerContent>
      <Toolbar class="sapMTBHeader-CTX">
        <Button text="Sair" type="Up" icon="sap-icon://log" iconFirst="true" width="auto" enabled="true" visible="true" press="onLogout"/>
      </Toolbar>
    </headerContent>
    <t:Table
        id="tblList"
        rows="{Data>/Items}"
        selectionMode="MultiToggle"
        selectionBehavior="RowSelector"
        alternateRowColors="true"
        enableColumnReordering="true"
        visibleRowCountMode="Auto"
        width="100%"
        enableCellFilter="true"
        rowHeight="40"
        rowSelectionChange="onRowSelectionChange"
        >
      <t:extension>
        <f:SimpleForm 
                    editable="true"
                    layout="ResponsiveGridLayout"
                    labelSpanL="4"
                    labelSpanM="4"
                    adjustLabelSpan="false"
                    emptySpanL="0"
                    emptySpanM="0"
                    columnsL="3"
                    columnsM="3"
                    singleContainerFullSize="false" >
          <f:content>
            <core:Title text="Parâmetros de consulta"/>
            <Label text="Data Criação de" />
            <DatePicker 
                            value="{Data>/DataIni}" 
                            valueFormat="yyyy-MM-dd"/>
            <Label text="Data Criação até" />
            <DatePicker 
                            value="{Data>/DataFin}" 
                            valueFormat="yyyy-MM-dd"/>
            <Label text="Tipo Etiqueta" />                            
            <Select 
                            forceSelection="false"
                            selectedKey="{Data>/tipoEtq}"
                            maxWidth="30rem"
                            items="{TiposEtiqueta>/Items}">
              <core:Item key="{TiposEtiqueta>tipoEtq}" text="{TiposEtiqueta>tipoEtq}" />
            </Select>
            
            <core:Title />
            <Label text="Item" />
            <Input 
                            value="{Data>/ItemCode}" />
            <Label text="Lote" />
            <Input 
                            value="{Data>/Lote}" />
            <Label text="Ordem de Produção" />
            <ComboBox 
                            selectedKey="{Data>/OP}"
                            maxWidth="30rem"
                            items="{OrdensProducao>/Items}">
              <core:Item key="{OrdensProducao>Documento}" text="{OrdensProducao>Documento}" />
            </ComboBox>

            <core:Title />
            <Label text="Estufa" />
            <Select 
                            forceSelection="false"
                            selectedKey="{Data>/Estufa}"
                            maxWidth="30rem"
                            items="{Estufas>/Items}">
              <core:Item key="{Estufas>APLATZ_ID}" text="{Estufas>APLATZ_ID}" />
            </Select>
            <Label text="Código da Caixa" />
            <Input 
                            value="{Data>/BoxCode}" />
            <Label text="Código do Pallet" />
            <Input 
                            value="{Data>/PalletCode}" />
          </f:content>
          <f:content>
            <core:Title />
            <HBox>
              <Button 
                            text="Filtrar" 
                            class="sapUiTinyMarginEnd"
                            width="125px"
                            press="onFilter" />
              <Button 
                            text="Limpar" 
                            class="sapUiTinyMarginEnd"
                            width="125px"
                            press="onClearForm" />
            </HBox>
            <core:Title />
            <Label text="Impressora"/>
            <Select
                            forceSelection="false"
                            selectedKey="{Data>/Impressora}"
                            maxWidth="30rem"
                            items="{Impressoras>/Items}">
              <core:Item key="{Impressoras>Impressora}" text="{Impressoras>Impressora}" />
            </Select>
            <core:Title />
            <Label text="Idioma Padrão"/>
            <HBox alignItems="Center">
              <Select
                              forceSelection="true"
                              selectedKey="{Data>/Idioma}"
                              maxWidth="26rem"
                              class="sapUiTinyMarginEnd">
                <core:Item key="PTB" text="Português Brasil" />
                <core:Item key="EN" text="Inglês" />
                <core:Item key="ES" text="Espanhol" />
              </Select>
              <Button 
                              icon="sap-icon://favorite" 
                              type="Transparent" 
                              tooltip="Salvar Idioma Padrão" 
                              press="onSaveFavoriteLang" />
            </HBox>
          </f:content>
        </f:SimpleForm>
      </t:extension>
      <t:columns></t:columns>
    </t:Table>
    <footer>
      <OverflowToolbar width="100%" >
        <content>
          <ToolbarSpacer />
          <Button text="Imprimir" press="onPrintPressed" type="Accept" icon="sap-icon://print"/>
          <Button text="Visualizar" press="onVisualizarPressed" type="Accept" icon="sap-icon://print" visible="{Data>/PodeVisualizar}"/>
        </content>
      </OverflowToolbar>
    </footer>
  </Page>
</mvc:View>